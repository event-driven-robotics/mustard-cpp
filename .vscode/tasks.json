{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build mustard_app",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --target mustard_app",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            },
            "problemMatcher": "$gcc"
        }
    ]
}
